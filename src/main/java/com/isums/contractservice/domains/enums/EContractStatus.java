package com.isums.contractservice.domains.enums;

public enum EContractStatus {
    DRAFT,
    READY,
    CONFIRM_BY_LANDLORD,
    IN_PROGRESS,
    CONFIRM_BY_TENANT,
    COMPLETED,
    CORRECTING,
    CANCELLED,
    REJECTED_BY_TENANT,
    REJECTED_BY_LANDLORD
}
